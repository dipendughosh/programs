#include <iostream.h>
#include <conio.h>
#include <stdlib.h>

int main()
{   char a,b;int len;
    while(cin.get(a))
    {cout<<"\nEnter a string : "; cin>>b;
    //len=strlen(a); cout<<"\tlen = "<<len;
    cout<<"\nYou entered : "<<b<<endl;}
    //for (int i=0;i<len;i++) cout<<*(a+i);
    getche();
    return 0;
}
