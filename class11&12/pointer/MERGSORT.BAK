#include<iostream.h>
#include<graphics.h>
#include<process.h>
#include<iomanip.h>
#include<string.h>
#include<conio.h>
#include<stdio.h>
#include<ctype.h>
#include<math.h>
#include<dos.h>

int one[]={1,4,3,2,5};
int two[]={6,9,10,8,7,11};
int a=sizeof(one)/2,b=sizeof(two)/2;

void main()
{	clrscr();
	int s=a+b;
	int three[20];
	void showarr(int [], int);
	int *small,*big,*temp;
	int sm,bg,x=0,y=0,count=0;
	showarr(one,a);
	showarr(two,b);
	if(a<=b)
	{	sm=a;
		bg=b;
		small=one;
		big=two;
	}
	else
	{	sm=b;
		bg=a;
		small=two;
		big=one;
	}
	int i=0;
	while(x<sm)
	{	if(small[x]==big[y])
		{	three[i++]=small[x++];
			three[i++]=big[y++];
		}
		else if(small[x]<big[y])
			three[i++]=small[x++];
		else
			three[i++]=big[y++];
	}
	while(i<=s)
		three[i++]=big[y++];
	showarr(three,i);
	getche();
}

void showarr(int arr[], int z)
{	for(int p=0;p<z;p++)
	{	cout<<arr[p]<<"\t";
	}
	cout<<"\n";
}






























































































































