#include<iostream.h>
#include<process.h>
#include<string.h>
#include<conio.h>
#include<stdio.h>
#include<math.h>
#include<ctype.h>

void main()
{	clrscr();
	int i,j,len,x=0,y=0,z=0;
	char name[80];
	cout<<"Enter namre ::";
	gets(name);
	len=strlen(name);
	for(i=0;i<len;i++)
	{	if(name[i]==' ')
			x++;
	}
	cout<<name[0]<<".";
	for(i=1;i<len;i++)
	{	if(name[i]==' ')
		{       while(name[i+1]==' ')
			{	i++;
				z=1;
			}
			if(z==1)
				i++;
			y++;
			if(y<x)
			{	cout<<name[i+1]<<".";
			}
			else
			{	for(j=i+1;j<len;j++)
					cout<<name[j];
				break;
			}
		}
	}
	//cout<<"\n\aP\aR\aE\aS\aS\a \aA\aN\aY\a \aK\aE\aY\a \aT\aO\a \aC\aO\aN\aT\aI\aN\aU\aE\a \a.\a.\a.\a.\a.\a.\a.\a.\a.\a.\a.\a.\a";
	getche();
}
