#include<iostream.h>
#include<conio.h>
#include<stdio.h>
#include<string.h>
void main()
{   clrscr();
		char string1[80],string2[80];
		int i,l,k,j;
		cout<<"Enter a string:";
		gets(string1);
		l=strlen(string1);
		string2[0]=' ';
		for(i=1;i<=l;i++)
			string2[i]=string1[i-1];
		k=l;
		for(i=l;i>=0;i--)
		{		if(string2[i]==' ')
				{	for(j=i;j<=k;j++)
					{	cout<<string2[j];
					}
					k=i;

				}
		 }
		 getch();
}