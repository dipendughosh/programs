DROP TABLE CUSTOMER;
CREATE TABLE CUSTOMER
(	CUST_ID VARCHAR2(10),
	CUST_NAME VARCHAR2(30),
	ANNUAL_REVENUE NUMBER(8,2) DEFAULT 20000,
	CUST_TYPE VARCHAR2(20),
	PRIMARY KEY(CUST_ID),
	CONSTRAINT CSTID CHECK(CUST_ID BETWEEN 10 AND 10000)
);
ALTER TABLE CUSTOMER ADD CONSTRAINT CSTTYPE CHECK(CUST_TYPE IN ('MANUFACTURER','WHOLESALER','RETAILER'));
INSERT INTO CUSTOMER(CUST_ID,CUST_NAME,ANNULA_REVENUE,CUST_TYPE) VALUES('','',,'');
DROP TABLE SHIPMENT;
CREATE TABLE SHIPMENT
(	SHIPMENT_NO INT,
	CUST_ID VARCHAR2(10),
	WEIGHT NUMBER(6,2) DEFAULT 10,
	TRUCK_NO VARCHAR2(10),
	DESTINATION VARCHAR2(10),
	SHIP_DATE DATE,
	PRIMARY KEY(SHIPMENT_NO)
);
ALTER TABLE SHIPMENT ADD CONSTRAINT WHGT CHECK(WEIGHT < 1000);
(,'',,'','',TO_DATE('','DD/MM/YYYY'));
DROP TABLE TRUCK;
CREATE TABLE TRUCK
(	TRUCK_NO VARCHAR2(10),
	DRIVER_NAME VARCHAR2(30),
	PRIMARY KEY(TRUCK_NO)
);


DROP TABLE CITY;
CREATE TABLE CITY
(	CITY_NAME VARCHAR2(10),
	POPULATION INT,
	PRIMARY KEY(CITY_NAME)
);

	
	