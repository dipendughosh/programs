#include<stdio.h>
#include<conio.h>

void main()
{	int y;
	clrscr();
	printf("ENTER A YEAR ");
	scanf("%d",y);
	if(y % 4==0)
	{   	if(y % 100!=0)
		{   	if(y % 400==0)
			{	printf(" Yes");
				getch();
				exit(0);
			}
			else
			{	printf(" No");
				getch();
				exit(0);
			}
		}
		else
		{	printf(" No");
				getch();
				exit(0);
		}
	}
	else
	{	printf(" Yes");
		getch();
		exit(0);
	}
	getch();
}

