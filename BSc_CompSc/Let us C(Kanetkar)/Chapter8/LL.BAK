#include<stdio.h>
#include<conio.h>
#include<math.h>

void main()
{       int a[10],i;
	float sum=0,x=0;
	clrscr();
	for(i=0;i<10;i++)
		scanf("%d",&a[i]);
	printf("x%f,sum%f\n",x,sum);
	for(i=0;i<10;i++)
	{      	x=pow(a[i],2);
		printf("x=%f",x);
		sum=sum+x;
		printf("sum=%f",sum);
		getch();
	}
	for(i=0;i<10;i++)
	{      	printf("%d\t",a[i]);
		printf("\n");
	}
	x=pow(sum,(1/2));
	printf("\n%f",x);
	getch();
}

