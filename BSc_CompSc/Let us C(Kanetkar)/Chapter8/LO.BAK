#include<stdio.h>
#include<conio.h>

void main()
{       float a[]={-6,-12,8,13,11,6,7,2,-6,-9,-10,11,10,9,2},i,m=0;
	clrscr();
	for(i=0;i<15;i++)
		m=m+a[i];
	m=m/15;
	for(i=0;i<15;i++)
	{	a[i]=a[i]-m;
		a[i]=a[i]*a[i];
	}
	m=0;
	for(i=0;i<15;i++)
		m=m+a[i];
	m=m/15;
	m=sqrt(m);
	printf("SD = %f",m);


	getch();
}

