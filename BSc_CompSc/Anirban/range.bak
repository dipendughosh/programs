# include <stdio.h>
# include <conio.h>
void main()
{
int n=0,i,max=1,min=100,r=0,x=0;
printf("enter limit");
scanf("%d",&n);
for(i=1;i<=n;i++)
{
printf("enter number");
scanf("%d",&x);
if(x>max)
max=x;
if(x<min)
min=x;
}
r=max-min;
printf("range is %d",r);
getch();
/*there is a problem for all negative inputs*/
}
