#include<stdio.h>
#include<conio.h>
#include<math.h>


void main()
{       char a[10];
	int i,x=0,j=0,len;
	clrscr();
	gets(a);
	len=strlen(a);
	for(i=len-1;i>=0;i--)
	{	x=x+(a[i]-48)*(pow(10,j));
		j++;

	}
	printf("\n%d",x);
	getch();
}

