//amit kumar kar-kar amit kumar
#include<stdio.h>
#include<conio.h>
#include<dos.h>
#include<string.h>

void main()
{	char str[80];
	int i,len,k,j;
	clrscr();
	printf("Enter name  ");
	gets(str);
	len=strlen(str);
	k=len-1;
	for(i=len;i>=0;i--)
	{	if(str[i]==' ')
		{	for(j=i;j<=k;j++)
				printf("%c",str[j]);
			break;
		}
	}
	for(j=0;j<=i;j++)
		printf("%c",str[j]);

	getch();
}

