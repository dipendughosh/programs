#include<stdio.h>
#include<conio.h>

void main()
{       int a[20],i,x,y;
	clrscr();
	for(i=0;i<10;i++)
	{	printf("Enter a[%d] ",i);
		scanf("%d",&a[i]);
	}
	for(i=0;i<10;i++)
		printf("\na[%d] = %d",i,a[i]);
	printf("\nEnter number to insert ");
	scanf("%d",&x);
	for(i=9;i>=0;i--)
	{	if(a[i]>=x)
			a[i+1]=a[i];
	}
	a[y]=x;
	for(i=0;i<11;i++)
		printf("\na[%d] = %d",i,a[i]);
	getch();
}

