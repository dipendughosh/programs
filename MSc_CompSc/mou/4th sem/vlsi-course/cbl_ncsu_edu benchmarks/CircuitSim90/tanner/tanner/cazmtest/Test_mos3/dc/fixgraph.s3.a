
BEGIN{ flag1 = 0 ; flag2 = 0 ; last_value = 0; }
{
     if ( (flag1 == 0) && ($1 == "Index") ) flag1 = 1;
     if ( (flag1 == 1) && ($1 == "0") ) {
	flag2 = 1;
	number = 0;
	}
     if ( ( flag2 == 1 ) && ( $1 == number ) ) {
	if ($2 < last_value) print "\" \"";
	print $2, "  ", ($3 * -1e6);
	number = number + 1;
	last_value = $2;
	}

}

